name: heygen
description: HeyGen integration (Live Avatar and related) for ai_interaction_sample.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

import 'package:flutter/material.dart';

/// Placeholder screen for HeyGen Live Avatar flow (wire UI here later).
class HeyGenLiveAvatarScreen extends StatelessWidget {
  const HeyGenLiveAvatarScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('HeyGen - Live Avatar')),
      body: const Center(child: Text('HeyGen Live Avatar')),
    );
  }
}
